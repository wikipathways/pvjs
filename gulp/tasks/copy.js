// Not sure what this task is doing or whether it's even needed.
var gulp = require('gulp');

gulp.task('copy', function(done) {
  return done();
  /*
	return gulp.src('./lib/cross-platform-text/dist/lib/**')
		.pipe(gulp.dest('./dist'));
  //*/
});
