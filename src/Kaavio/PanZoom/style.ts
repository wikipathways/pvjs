import {style, cssRaw} from 'typestyle';

cssRaw(`
    @import('boot)
`);

export const styles = {
    zoomControlsWrapper: {
        position: 'absolute',
        right: 0
    },
    zoomControl: {
        'margin-right': '1rem'
    }
};